[tool.poetry]
name = "pyjobby"
version = "1.2.3"
description = "PostgreSQL Job Consumer Platform"
authors = ["Matt Stancliff <matt@genges.com>"]

[tool.poetry.dependencies]
python = "^3.10"
orjson = "^3.5.0"
click = "^8.0.0"
asyncpg = "^0.29.0"
loguru = "^0.5.3"
aiohttp = "^3.7.3"

[tool.poetry.scripts]
pj = "pyjobby.pj:workit"

[tool.poetry.group.test.dependencies]
pytest = "^8.0"
pytest-asyncio = "^0.23"
pytest-cov = "^4.1"
pytest-timeout = "^2.2"
pytest-xdist = "^3.5"
hypothesis = "^6.148.1"

[tool.poetry.group.dev.dependencies]
mypy = "^1.8"
ruff = "^0.1"
black = "^24.0"
types-orjson = "^3.6"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "-ra",
    "--strict-markers",
    "--strict-config",
    "--cov=pyjobby",
    "--cov-report=term-missing:skip-covered",
    "--cov-report=html",
    "--cov-report=xml",
]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests",
    "concurrency: marks tests as concurrency tests",
    "stress: marks tests as stress tests (high load scenarios)",
    "hypothesis: marks tests as property-based tests using Hypothesis",
]
timeout = 300
filterwarnings = [
    "error",
    "ignore::DeprecationWarning",
]

[tool.coverage.run]
source = ["pyjobby"]
omit = ["*/tests/*", "*/test_*.py"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
    "@abstractmethod",
]

[tool.mypy]
python_version = "3.9"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
strict_equality = true

[tool.ruff]
line-length = 100
target-version = "py39"
select = ["E", "F", "I", "N", "W", "UP", "B", "C4", "SIM"]
ignore = ["E501"]  # Line too long (handled by black)

[tool.black]
line-length = 100
target-version = ["py39"]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
